THIS FILE IS FOR ME ONLY SO ANYONE THAT THINKS THIS IS A SCUFFED TICKETING SYSTEM... WELL IT WORKS SO GET OVER IT

(x, y, z)

x: ticket is on this project
y: id of ticket (based on project, i.e. project 1 and 2, both have a ticket with ID 0)
z: ticket depends on this other ticket


TICKETS:
[x] (1, 0, -) -> remove tilemap_access function and just map tilemap memory directly to a struct, with counter, width/height, tile data for easy access
[x] (1, 1, 0) -> refactor input handling
[x] (1, 2, 0) -> FEATURE: adjustable Red, Blue, Green unit counters in editor (i.e. editor can change how many RGB units can be placed for the level being edited)
[x] (1, 3, 0) -> FEATURE: adjust size of editor's tiles based on width/height of tilemap
[x] (1, 7, 0) -> if editor is passed nonexistent file, just create a new level with width 1, height 1 or smn
[x] (1, 4, -) -> BUGFIX: allow tilegame to reload levels that have just been edited by editor without breaking
				 - reallocate memory when reloading a tilemap (tilemap memory must be @ end of game_memory, so it can be freed, reallocated)
				 - make sure rendered tile size is adjusted after reload and is based on MAX(width, height) rather than width every time. that way any level can fit on screen correctly

*** remaining stuff needed for demo is below ***
[ ] (1, 5, -) finish demo
	[ ] tilegame.c rewrite (rewrite all at once and get all these features in rather than trying to knock out one
		at a time is prob best way to do it. think of each of these as a system that you should program in a way
		that makes iteration/later changes very easy, and doesn't lock down the game rules early on)
		* YOU SHOULD JUST STRAIGHT DELETE 'tilegame.c' AND DO AN ACTUAL FULL RE-WRITE
		- bunch of todos with rendering and initialization of map and meshes
		- recursive effect -> a changed unit then triggers a "move" as if the unit was placed
			- maybe try to avoid making levels that use this concept in the demo
			- it's also totally fine if you decide this is a horrible idea
				- it might be a horrible idea
		- additional tile/unit types with different shift targets and stuff
			- potentially change behaviours of red/green/blue tiles
			- again, you should avoid using these additional types in the demo, but I just want
			a re-write so I can have all the systems I want in place
		- system to have level-specific text thats stored in a file render in each level
		- system to go through levels in order
			- on level completion, option to play again ('spacebar') or go to next ('n') or smn like that
			- just need this system in place for the demo, it doesn't have to work great. eventually we'd want
			some sort of level menu 
		- basic UI -> display unit counters in a fun way like have a colored square with a number next to it
			- make sure player knows goal is to eliminate red squares
			- display instructions for that lvl + controls ('r', 'g', 'b')
			- make it clear to player when they can make a move (i.e. previous move is done processing)
	[ ] demo levels
		- make sure they work as expected after tilegame.c changes
		- write more, make sure they have clear instructions and flow in an order that makes sense

[x] (2, 0, -) -> windows platform layer
[x] (2, 1, -) -> win32_util.c

*** stuff below not needed for demo ***
[ ] (3, 0, -) -> UI code
				 - menus, buttons, stuff you'll need for UI. KEEP VERY SIMPLE
[ ] (3, 1, 0) -> UI for game
				 - refactor existing UI code or just delete it (prob delete LOL)
				 - level select, fade out of levels when win and go back to level select
				 - clear instructions on how to play
[ ] (3, 2, 0) -> UI for editor (honestly totally optional for completing a demo bc only you are using editor it can be as scuffed as u want as long as it works)
[ ] (4, 0, -) -> win32 sound system
[ ] (4, 1, -) -> linux sound system
[ ] (4; 2; 0,1) -> sounds for tilegame

PROJECTS:
1 Editor + 10 intro/tutorial/demo levels
2 Windows Port
3 UI
4 sound (doubt I'll do it on this project tbh)
